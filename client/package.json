{
  "name": "client",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "jest --config jest.config.cjs --coverage --ci --testResultsProcessor=jest-junit"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^30.2.0",
    "jest-junit": "^16.0.0",
    "@types/jest": "^30.0.0"
  }
}
